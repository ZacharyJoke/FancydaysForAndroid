<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fancydaysforandroid"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="23" />

    <application
        
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" android:vmSafeMode="true">
        <activity
            android:name="com.app.fancyday.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
             android:name="com.app.fancyday.AddActivity"
             android:label="@string/app_name">
      
        </activity>
            <activity   
                android:name="com.app.fancyday.ShowActivity"
                android:label="@string/app_name">
          \
            </activity>
           
        
    </application>

</manifest>
